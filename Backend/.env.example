PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret_value
# Frontend origin for CORS (Vite default is 5173)
CLIENT_ORIGIN=http://localhost:5173

# MongoDB Atlas: copy from Atlas → Connect → Drivers → Connection string.
# If your password contains @ or #, URL-encode them (@ → %40, # → %23).
# Add your database name after .net/ or set MONGO_DB_NAME.
MONGO_URL=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGO_DB_NAME=se_project
